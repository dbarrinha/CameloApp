import React from 'react';
import { View } from 'react-native';
import { Card, CardInfo, Description, TextFooter, Thumb, Title } from './styles';

export default function ListaFavs() {

    const renderFav = ()=>{

        return 
    }

    return (
        <View />
    );
}
